# ============================================
# Table Extractor 환경 설정 템플릿
# ============================================
# 사용 방법:
# 1. 이 파일을 복사하여 .env 파일 생성
#    cp .env.example .env
# 2. 아래 설정값을 실제 환경에 맞게 수정
# ============================================

# ============================================
# LLM Configuration
# ============================================

# LLM 서버 URL (OpenAI API 호환 서버)
# 예시: http://your-server:9393 또는 https://api.openai.com
LLM_SERVER_URL=http://your-llm-server:port

# 사용할 LLM 모델명
# 지원 모델:
#   - o3-mini (추천: 추론 능력 우수, 200K 컨텍스트)
#   - o3 (최고 성능)
#   - gpt-4o-mini (빠르고 저렴, 128K 컨텍스트)
#   - gpt-4o (정확도 높음)
LLM_MODEL_NAME=o3-mini

# LLM API 타임아웃 (밀리초)
# 긴 쿼리 분석 시 충분한 시간 설정 권장
LLM_TIMEOUT_MS=60000

# Temperature (0.0 ~ 1.0)
# 낮을수록 일관성 있는 응답, 높을수록 창의적인 응답
# 참고: o3 계열은 temperature 미지원
LLM_TEMPERATURE=0.2

# 최대 출력 토큰 수 (gpt-4o 계열용)
# 대부분의 JSON 응답에는 4096이면 충분
LLM_MAX_TOKENS=16384

# 최대 출력 토큰 수 (o3 계열용)
# o3-mini는 max_completion_tokens 사용
# 권장: 4096 ~ 16384 (JSON 응답에 충분)
LLM_MAX_COMPLETION_TOKENS=16384

# ============================================
# MySQL Configuration
# ============================================

# MySQL 데이터베이스 연결 URL
# 형식: jdbc:mysql://HOST:PORT/DATABASE?옵션
# 예시: jdbc:mysql://localhost:3306/table_lineage?useSSL=false&serverTimezone=UTC
MYSQL_URL=jdbc:mysql://your-mysql-host:3306/your_database?useSSL=true&serverTimezone=Asia/Seoul&connectTimeout=30000&autoCommit=true&allowPublicKeyRetrieval=true&verifyServerCertificate=false

# MySQL 사용자명
MYSQL_USERNAME=your_username

# MySQL 비밀번호
# 주의: .env 파일은 반드시 .gitignore에 포함시켜야 합니다!
MYSQL_PASSWORD=your_password

# MySQL JDBC 드라이버 (기본값 사용 권장)
MYSQL_DRIVER=com.mysql.cj.jdbc.Driver

# ============================================
# 보안 참고사항
# ============================================
# 1. .env 파일은 절대 Git에 커밋하지 마세요!
# 2. 프로덕션 환경에서는 비밀번호를 환경변수로 관리하세요
# 3. 데이터베이스 접근 권한을 최소화하세요
# 4. LLM 서버 URL이 외부에 노출되지 않도록 주의하세요
